# 2026-06-27

## Native iMessage Migration Complete

Dylan completed a major OpenClaw update batch, culminating in migrating from BlueBubbles to native iMessage (`imsg 0.11.1`).

**Key changes:**
- OpenClaw upgraded to **2026.6.10** (aa69b12)
- iMessage now handled by native `imsg` bridge (advanced bridge, SIP disabled for BB parity)
- BlueBubbles remains installed as rollback/soak coverage only — no longer primary
- Cron migrated to SQLite: 38 live jobs, 25 iMessage deliveries, 0 BlueBubbles deliveries
- Cron model IDs normalized from `openai-codex/gpt-5.5` → `openai/gpt-5.5`
- `openclaw.json` symlinked back to `openclaw/openclaw.json`
- Migration runbook: `openclaw/plans/openclaw-2026-6-native-imessage-migration.md`
- Backup: `~/.openclaw/backups/openclaw-2026-6-migration-20260627T132011Z`

**Verified working:**
- Gateway iMessage account running, no errors, no restart pending
- Tapbacks/reactions ingested properly
- All validators passing (config validate, sync validate, jq, bash, py_compile, git diff)

**Known non-issues:**
- `install.sh --dry-run` has pre-existing non-interactive conflict warnings (not migration-related)
- Tailscale serve warning due to Tailscale being logged out (separate issue)

**Impact on workspace docs:**
- TOOLS.md BlueBubbles section needs updating (BB no longer primary)
- BB watchdog, webhook architecture, routing notes now historical
- SOUL.md BlueBubbles routing section may need revision for native iMessage

## Google Drive Organization — First Pass

Dylan asked Claude to organize his Google Drive. A conservative, non-destructive first pass was completed:

- Root inventory before changes: 303 items (22 folders, 281 loose files).
- Created private root folders `Legal – Maselli v. Wiberg (Working Files)` and `Homes`.
- Moved 11 clearly related Maselli/Wiberg case files into the private legal working folder without changing permissions.
- Filed obvious recent items into existing folders: Amsterdam map → Trips, Cursor application doc → Resume / CV, Finance Info → Financial Advisor, cabin Title V report → Homes.
- Consolidated duplicate Writing folders: older folder nested and renamed `Archive – Older Writing`; recent family letter and Coconut/Potato picture book moved into Writing.
- Root now has 287 items (23 folders, 264 loose files); no files were deleted.
- Verification caught six case files unexpectedly marked trashed after the move API calls. They were immediately restored in place. Final audit confirmed all 18 moved items are untrashed, all 11 legal files are present, and there are no newly trashed files.
